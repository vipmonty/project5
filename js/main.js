/**
 * Main JavaScript Logic for the VIP Photography Website.
 * Author: Viphakone Monty
 */

document.addEventListener("DOMContentLoaded", function () {
    // Initialize validation for the visitor form
    initValidation("#visitor-form");

    // Initialize page logic (e.g., theme toggling, navigation)
    initPageLogic();
});
